import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const faceScanSlice = createSlice({
  name: "faceScanSlice",
  initialState,
  reducers: {},
});

export const {} = faceScanSlice.actions;

export default faceScanSlice.reducer;
